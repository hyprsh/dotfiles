# my dotfiles

Make shure to run `./install.sh` before using the dotfiles.

> Be shure to run this script from within the repository.
> It will create symlinks in your home directory.

```sh
# to gather current config
./gather.sh

# to install config
./install.sh
```
